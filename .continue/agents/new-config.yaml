name: Local Ollama Config
version: 1.0.0
schema: v1

models:
  - name: Qwen2.5 Coder 14B (Local)
    provider: ollama  # เปลี่ยนจาก deepseek เป็น ollama
    model: qwen2.5-coder:14b
    apiKey: ""  # ไม่ต้องใช้ API Key เพราะเป็น Local
    baseUrl: http://localhost:11434  # URL ของ Ollama
